import {z} from 'zod';import {validateMainnetIntent} from './intent';
const hex=z.string().regex(/^(?:[a-f0-9]{2})+$/),hash=z.string().regex(/^[a-f0-9]{64}$/);
const verified=z.object({format:z.literal('qsb-mainnet-verified-helper-result-v1'),network:z.literal('mainnet'),intentHash:hash,rawTxHex:hex,txid:hash,helperSignatureVerified:z.literal(true),qsbConsensusProven:z.literal(false),chainInclusionProven:z.literal(false),broadcastAuthorized:z.literal(false)});
/** Allowlist only public fields from the trusted finalizer. Never serializes recovery or backup state. */
export function publicSignedResult(contract:unknown,result:unknown){const bound=validateMainnetIntent(contract),v=verified.parse(result);if(v.intentHash!==bound.intentHash)throw Error('Returned signature intent differs');return {format:'qsb-mainnet-public-signed-result-v1' as const,network:'mainnet' as const,requestId:bound.contract.manifest.vaultId,jobId:bound.contract.manifest.idempotencyKey,intentHash:v.intentHash,rawTxHex:v.rawTxHex,txid:v.txid,helperSignatureVerified:v.helperSignatureVerified,qsbConsensusProven:v.qsbConsensusProven,chainInclusionProven:v.chainInclusionProven,broadcastAuthorized:v.broadcastAuthorized};}
