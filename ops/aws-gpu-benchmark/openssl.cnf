# Ubuntu 22.04's Python/OpenSSL needs the legacy provider for Bitcoin HASH160.
openssl_conf = openssl_init
[openssl_init]
providers = providers
[providers]
default = default_provider
legacy = legacy_provider
[default_provider]
activate = 1
[legacy_provider]
activate = 1
